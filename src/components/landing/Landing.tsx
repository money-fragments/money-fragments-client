import { CustomButton } from 'components/common/ButtonComponents';
import React from 'react';

const Landing = () => {
  return (
    <div>
      <h1>loading..</h1>
    </div>
  );
};

export default Landing;
